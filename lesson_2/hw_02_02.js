/* 
2. Чему будет равен x?

	var a = 2;
	var x = 1 + (a *= 2);
*/

// x равен 5
// сначала вычисляется действие в скобках, где значение переменной a умножается на 2 (результат - 4)
// затем к 1 прибавляется полученное значение (1 + 4 = 5), которое присваивается переменной x
